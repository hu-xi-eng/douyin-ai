将「图2 厨房插画」保存为 kitchen.png 放入本目录。

要求：
- 文件名：kitchen.png（或同时支持 kitchen.jpg，再到 css/style.css 改后缀）
- 比例：建议 9:16（750x1334 / 1080x1920 等），css 设置为 cover，会按容器裁剪居中
- 不要在 PS / 设计稿里加任何滤镜，引用方式严格 cover + center，无 filter

文件就位后无需改代码，刷新页面即可生效。
